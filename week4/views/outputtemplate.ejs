<html>
    <head>
        <title>data˜˜˜˜˜</title>
        <style>
            body{
                font-size: 4.5vh; 
                font-family: 'Courier New', Courier, monospace; 
                position: absolute; 
                top: 2vh; 
                right: 2vw; 
                left: 2vw; 
                text-align: justify;
             }

        </style>
        <!--
        <script type="text/javascript">
        window.addEventListener('load',init);
       function init(){
           let currentDate=new Date();
           let date=currentDate.getFullYear()+'-0'+(currentDate.getMonth()+1)+'-'+currentDate.getDate(); 
           console.log(date);
           document.getElementById("dateInput").value = date;
           document.getElementById("dateInput2").value = date;
       }
        </script>-->
    </head>
    <body>
        <form method="POST" action="/formdata">
        <div>
            <form method="GET" action="/search">
                <% let currentDate=new Date(); %>
                <% let date=currentDate.getFullYear()+'-0'+(currentDate.getMonth()+1)+'-'+currentDate.getDate(); %>
                
                <input type="date" id="dateInput" name="q" value="<%=date %>" min="2021-01-01" max="2021-12-31">
                <input id="button" type="submit" value="search">
            </form>
        </div>
        <% if(data.length>0){ %>
            <% for(var i = 0; i<data.length; i++){ %>
                <span id="dataout<%=i%>"><a style="color: <%=data[i].color %>; text-decoration:none;"  href="/displayrecord?_id=<%= data[i]._id %>""><%=data[i].emoji+data[i].date%></a></span>
          <%  } %>
        <% } %>
                
        
    </body>
</html>